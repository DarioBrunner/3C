<%= form_with(model: user_to_company, local: true) do |form| %>
  <% if user_to_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_to_company.errors.count, "error") %> prohibited this user_to_company from being saved:</h2>

      <ul>
      <% user_to_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin %>
  </div>

  <div class="field">
    <%= form.label :user_id_id %>
    <%= form.text_field :user_id_id %>
  </div>

  <div class="field">
    <%= form.label :company_id_id %>
    <%= form.text_field :company_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
