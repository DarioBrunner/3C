<%= form_with(model: group_comment, local: true) do |form| %>
  <% if group_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_comment.errors.count, "error") %> prohibited this group_comment from being saved:</h2>

      <ul>
        <% group_comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field  form-group">
    <%= form.label :group_message_id %>
    <%= form.text_field :group_message_id %>
  </div>

  <div class="field  form-group">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions  form-group">
    <%= form.submit %>
  </div>
<% end %>

